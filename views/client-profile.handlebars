{{!-- image placeholder of individual client --}}

{{!-- Should display most of the following:
First name
Last name
Email
Password
Age
Gender
Weight
Height
Phone number
Goals
Injuries
Medical conditions
Diet
History
Working Notes (Brian notes of sessions)
Plan type
 --}}
<div class="row">
    <div class="col s8">
        <h3 class="header">{{dataValues.first_name}} {{dataValues.last_name}}</h3>
        <div class="card horizontal ">
            <div class="card-image">
                <img src="https://lorempixel.com/100/190/nature/6">
            </div>
            <div class="card-stacked">
                <div class="card-content ">
                    <p id="user_id">User ID:</p>
                    <p id="plan_type">Plan Type:{{dataValues.plan_type}}</p>
                    <p id="age">Age:{{dataValues.age}}</p>
                    <p id="gender">Gender:{{dataValues.gender}}:</p>
                    <p id="weight">User:{{dataValues.user_weight}}:</p>
                    <p id="height">Height:{{dataValues.user_height}}</p>
                    <p id="phone">Phone:{{dataValues.phone_number}}</p>
                    <p id="medical_conditions">Medical Conditions:{{dataValues.medical_conditions}}</p>
                </div>
                <div class="card-tabs">
                    <ul class="tabs tabs-fixed-widthz">
                        <li class="tab"><a href="#goals">Goals</a></li>
                        <li class="tab"><a href="#injuries">Injuries</a></li>
                        <li class="tab"><a href="#diet">Diet</a></li>
                        <li class="tab"><a href="#history">History</a></li>
                    </ul>
                </div>
                <div class="card-content grey lighten-4">
                    <div id="goals">{{dataValues.goals}}</div>
                    <div id="injuries">{{dataValues.injuries}}/div>
                        <div id="diet">{{dataValues.diet}}</div>
                        <div id="history">{{dataValues.history}}</div>
                    </div>
                    <div class="card-action">
                        <a id="add-note" href="#">Add Note</a>

                    </div>
                    <div class="card-action">
                        <a id="email" href="#">Contact/Email</a>
                    </div>
                </div>
            </div>
        </div>

        {{!-- Text Area for "Add Note" --}}
        <div class="row">
            <form class="col s12">
                <div class="row">
                    <div class="input-field col s12">
                        <textarea id="textarea1" class="materialize-textarea"></textarea>
                        <label for="textarea1">Add Note</label>
                    </div>
                </div>
            </form>
        </div>
    </div>
    {{!-- Session Notes --}}
    <div class="row">
        <div class="col s12 m6">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title">Date of Session</span>
                    <p>I am a very simple card. I am where you will store session notes. {{dataValues.session_notes}}
                    </p>
                </div>
                <div class="card-action">
                    <a href="#">Edit Note</a>
                    <a href="#">Delete Notek</a>
                </div>
            </div>
        </div>
    </div>

    <div class="container">
        <!-- Page Content goes here -->
        <!-- Client's Profile -->
        <div class="row s6 align-center">
            <div class="col s10 push-s1">
                <div class="card horizontal ">
                    <div class="card-image">
                        <img src="https://lorempixel.com/100/190/nature/6">
                    </div>
                    <div class="card-stacked">
                        <div class="card-content ">
                            <h4 id="name">{{dataValues.first_name}} {{dataValues.last_name}}</h4>
                            <p id="user_id">User ID:</p>
                            <p id="plan_type">Plan Type:{{dataValues.plan_type}}</p>
                            <p id="age">Age:{{dataValues.age}}</p>
                            <p id="gender">Gender:{{dataValues.gender}}:</p>
                            <p id="weight">Weight:{{dataValues.user_weight}}:</p>
                            <p id="height">Height:{{dataValues.user_height}}</p>
                            <p id="phone">Phone:{{dataValues.phone_number}}</p>
                        </div>
                        <div class="card-tabs">
                            <ul class="tabs tabs-fixed-widthz">
                                <li class="tab"><a href="#goals">Goals</a></li>
                                <li class="tab"><a href="#injuries">Injuries</a></li>
                                <li class="tab"><a href="#medical_conditions">Medical Conditions</a></li>
                                <li class="tab"><a href="#diet">Diet</a></li>
                                <li class="tab"><a href="#history">History</a></li>
                            </ul>
                        </div>
                        <div class="card-content grey lighten-4">
                            <div id="goals">{{dataValues.goals}}</div>
                            <div id="injuries">{{dataValues.injuries}}</div>
                            <div id="medical_conditions">None</div>
                            <div id="diet">{{dataValues.diet}}</div>
                            <div id="history">{{dataValues.history}}</div>

                        </div>
                        <div class="card-action">
                            <a id="add-note" href="#">Add Note</a>
                        </div>
                        <div class="card-action">
                            <a id="email" href="#">Contact/Email</a>
                        </div>
                    </div>
                </div>

            </div>

        </div>
        <!-- Add Client Note & Submit -->
        <div class="row">
            <div class="divider"></div>
            <div class="section">
                <h5>Add Note</h5>
                <div class="row">
                    <form class="col s12">
                        <div class="row">
                            <div class="input-field col s6">
                                <i class="material-icons prefix">mode_edit</i>
                                <textarea id="icon_prefix2" class="materialize-textarea"></textarea>
                                <label for="icon_prefix2">Add Client Note Here </label>
                            </div>

                        </div>
                    </form>
                    <button class="btn waves-effect waves-light" type="submit" name="action">Submit
                        <i class="material-icons right">send</i>
                    </button>
                </div>
            </div>
            <div class="divider"></div>
        </div>

        <!-- All Saved Notes go to Cards here. -->
        <div class="row">

            <div class="section">
                <h5>Notes</h5>
                <div class="col s4 ">
                    <div class="card blue-grey darken-1">
                        <div class="card-content white-text">
                            <span class="card-title">Date of Session 06/06/20 </span>
                            <p>I am a very simple card. I am where you will store session notes.
                                {{session_notes}}</p>
                        </div>
                        <div class="card-action">
                            <a href="#">Edit Note</a>
                            <a href="#">Delete Note</a>
                        </div>
                    </div>
                </div>

            </div>

        </div>
    </div>
    <div>